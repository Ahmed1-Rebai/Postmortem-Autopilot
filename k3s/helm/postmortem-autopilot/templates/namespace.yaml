# Separating postmortem-data means the app namespace can be torn down and
# reapplied freely during development without touching the graph (docs/05).
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespace.app }}
---
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespace.data }}
